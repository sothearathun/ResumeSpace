// Set NEXT_PUBLIC_SITE_URL once this is deployed to a real domain — the
// sitemap, robots.txt, and post metadata all resolve absolute URLs from it.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://resumecraft.example.com";
