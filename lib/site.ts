// Override with NEXT_PUBLIC_SITE_URL if the domain ever changes — the
// sitemap, robots.txt, and post metadata all resolve absolute URLs from it.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://resumespace.site";
