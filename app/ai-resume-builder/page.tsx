import { SeoLandingPage, seoPageMetadata } from "@/components/seo/SeoLandingPage";

export const metadata = seoPageMetadata("ai-resume-builder");

export default function Page() {
  return <SeoLandingPage slug="ai-resume-builder" />;
}
