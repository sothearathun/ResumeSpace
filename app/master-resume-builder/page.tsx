import { SeoLandingPage, seoPageMetadata } from "@/components/seo/SeoLandingPage";

export const metadata = seoPageMetadata("master-resume-builder");

export default function Page() {
  return <SeoLandingPage slug="master-resume-builder" />;
}
