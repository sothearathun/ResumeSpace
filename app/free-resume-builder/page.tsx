import { SeoLandingPage, seoPageMetadata } from "@/components/seo/SeoLandingPage";

export const metadata = seoPageMetadata("free-resume-builder");

export default function Page() {
  return <SeoLandingPage slug="free-resume-builder" />;
}
