import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ResumeExamplesPage() {
  return (
    <ComingSoon
      title="Resume examples are on the way."
      description="A library of real, role-specific resume examples — coming after the builder ships."
    />
  );
}
